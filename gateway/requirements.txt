# Gateway runtime dependencies.
# web3 is required: without it blockchain_bridge falls back to
# WEB3_AVAILABLE = False and the on-chain path is skipped silently.
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
joblib>=1.3.0
httpx>=0.26.0
requests>=2.31.0
web3>=6.0.0
pytest>=8.0.0
